
Lösung von konkreter KW in der gesmaten Logik

GET/POST /mealplan/{date} -> datum in der Woche (am besten Motang)

Frontend greift nur noch über das Datum einer Woche auf das Backend zu. KW Berechnung nur noch im Frontend. KW schwerer zu berechnen

Entity:

Mealplan:
    - start_date
    - end_date
    - ...

order passt eigentlich mit dem OrderDTO. Nur vom Tag Abhänig
